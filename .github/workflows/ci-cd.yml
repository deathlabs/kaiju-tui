---
name: CI/CD

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:

  secrets:
    name: Secrets Scan
    runs-on: ubuntu-latest
    steps:
      - name: Clone the repository
        uses: actions/checkout@v6
      - name: Run Trufflehog
        id: trufflehog
        uses: trufflesecurity/trufflehog@main
        continue-on-error: false

  code-lint:
    name: Code Lint Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.2'
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9.3.0
        with:
          args: --output.sarif.path kaiju-tui-lint-scan.sarif
      - name: Print Code Lint Scan
        if: always()
        run: cat kaiju-tui-lint-scan.sarif

  sast:
    name: SAST Scan
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    steps:
      - name: Clone Repository
        uses: actions/checkout@v7
      - name: Run Semgrep
        run: semgrep scan --config auto . --sarif --sarif-output=kaiju-tui-sast-scan.sarif
      - name: Print SAST Scan
        if: always()
        run: cat kaiju-tui-sast-scan.sarif
      - name: Upload SAST Scan
        if: always()
        uses: actions/upload-artifact@v7
        with:
          path: kaiju-tui-sast-scan.sarif
          archive: false

  sca:
    name: SBOM Scan
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
    steps:
      - name: Clone Repository
        uses: actions/checkout@v6
      - name: Run Syft
        uses: anchore/sbom-action@v0
        with:
          path: .
          format: cyclonedx-json
          output-file: kaiju-tui-sbom.json
          artifact-name: kaiju-tui-sbom.json
      - name: Run Grype
        id: grype
        uses: anchore/scan-action@v7.4.0
        with:
          sbom: kaiju-tui-sbom.json
          vex: ${{ hashFiles('.github/vex/kaiju-tui-vex.json') != '' && '.github/vex/kaiju-tui-vex.json' || '' }}
          severity-cutoff: ${{ github.event.inputs.sbom_scan_failure_threshold || 'low' }}
          output-format: cyclonedx-json
          output-file: kaiju-tui-sbom-scan.json
          fail-build: true
      - name: Print SBOM Scan
        if: always()
        run: jq -r '
            (.vulnerabilities // [])[] |
            [
              ((.ratings[]? | select(.severity != null) | .severity) // "unknown"),
              .id,
              (try ((.affects[0].ref // "") | capture("pkg:(?<pkg>[^?]+)").pkg) catch "unknown")
            ] | @tsv
          ' kaiju-tui-sbom-scan.json | sort | column -t
      - name: Upload SBOM Scan
        if: always()
        uses: actions/upload-artifact@v7
        with:
          path: kaiju-tui-sbom-scan.json
          archive: false

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [ secrets, code-lint, sast, sca ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.2'
      - name: Build kaiju-tui
        run: |
          CGO_ENABLED=0 GOOS=linux   GOARCH=amd64 go build -o dist/kaiju-tui-linux-amd64       .
          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -o dist/kaiju-tui-windows-amd64.exe .
      - name: Extract release notes
        id: release_notes
        uses: ffurrer2/extract-release-notes@v2
      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body: ${{ steps.release_notes.outputs.release_notes }}
          files: dist/*
          draft: false
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') }}
